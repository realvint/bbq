<% flash.each do |type, message| %>
  <div id="alert" class="alert <%= bootstrap_class_for_flash(type) %> alert-dismissible" role="alert">
    <%= sanitize(message) %>
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
  </div>
<% end %>
