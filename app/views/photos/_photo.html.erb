<div class="event-photo pe-1">
  <%= link_to image_tag(photo.photo.thumb.url), photo.photo.url %> <br />
  <div class="text-small"><%= t('.creator') %></div>
  <%= render partial: 'users/user', object: photo.user %>
</div>
